services:
  laan:
    image: laan:latest
    container_name: laan
    ports:
      - "5000:5000"
      - "7070:7070"
    volumes:
      - laan-data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  laan-data:
    name: laan-data
